name: flutter_certilia
description: "Flutter SDK for Certilia Identity API integration. Supports OAuth 2.0 authentication with Croatian eID (eOsobna) through NIAS system."
version: 0.2.0
homepage: https://github.com/stepanic/flutter_certilia
repository: https://github.com/stepanic/flutter_certilia

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^10.3.1
  webview_flutter: ^4.8.0
  web: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.0

flutter:
